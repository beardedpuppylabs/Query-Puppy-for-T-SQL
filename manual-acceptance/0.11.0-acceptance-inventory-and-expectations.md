# Query Puppy for T-SQL 0.11.0 Manual Acceptance Inventory

Reviewed repository state: `package.json` version `0.11.0`, `AGENTS.md`,
`docs/ARCHITECTURE.md`, `docs/COMPLETION_PIPELINE.md`,
`docs/TYPE_SYSTEM.md`, `docs/TESTING.md`, `docs/DEVELOPMENT.md`,
`docs/IMPLEMENTATION_PLAN.md`, `README.md`, `CHANGELOG.md`, automated tests,
Extension Host tests, and existing SQL fixture scripts.

Use:

- `extend-intellisenselab-0.11.0-fixtures.sql` only if the stated fixture gaps are
  present. Run it manually; Codex must not execute DDL.
- `0.11.0-manual-test-cases.sql` for copy/paste manual Suggest and Signature Help
  checks.
- Count only semantic Query Puppy for T-SQL items. Ignore generic editor word
  suggestions with an `abc` icon.

## Feature Inventory

### Editor Integration And Configuration

- VS Code/VSCodium SQL activation on `onLanguage:sql`.
- Native CompletionItemProvider and SignatureHelpProvider.
- Native Suggest UI only; no custom editor UI and no Microsoft completion
  scraping/post-filtering.
- Commands: expand `SELECT *`, refresh metadata, clear active database cache,
  show status, disable Microsoft SQL suggestions, diagnose Signature Help,
  diagnose Query Scope.
- Settings: extension enablement, debug logging, Smart Alias enablement.
- Tab-only wildcard expansion context key; Enter must not expand.
- Microsoft `mssql` suggestions are handled by explicit user consent/command only.

### MSSQL Connection And Metadata Lifecycle

- Reuses the active Microsoft `ms-mssql.mssql` connection-sharing API.
- Active connection/database lookup remains dynamic.
- mssql API acquisition and active-context lookups are coalesced and retryable.
- Catalog loads are read-only, set-based, per connection/database identity.
- Secondary databases are loaded lazily only after explicit qualification.
- Versioned persistent canonical snapshots live in `globalStorageUri`.
- Snapshot identity is hash-based from connection ID plus database name.
- Warm hydration rebuilds runtime `DatabaseIndex` immediately.
- First use per extension session schedules one non-blocking refresh.
- Later refresh eligibility is demand-driven after 15 minutes.
- Manual refresh bypasses freshness but uses the same coalesced refresh path.
- Refresh failures preserve the last usable memory and disk snapshot.
- Cache clear is active-database scoped and confirmed.

### Completion Pipeline

- Matching is case-insensitive contiguous Contains.
- No StartsWith-only, fuzzy, prefix bonus, or substring-position bonus behavior.
- Explicit exact textual priority may rank the exact item first.
- Deterministic sorting within equivalent semantic/ranking tiers.
- Explicit qualifier resolution is strict: `alias.` returns only that RowSource.
- Semantic candidates are created before filtering/ranking/materialization.
- Physical columns use one canonical CompletionItem factory.
- Physical-column rows preserve complete filterText, insertText, metadata and docs.
- Long names are visibly bounded but insert/filter using the complete identifier.
- Type groups are decorative, insert nothing, and never become semantic columns.

### Row-Source And Object Completion

- `FROM`, `JOIN`, and `APPLY` source completion includes schemas, tables, views,
  TVFs, synonyms where loaded, and visible local RowSources.
- Active-database schemas participate in unqualified source completion.
- Database-qualified forms include `Database.`, `Database.Schema.`,
  `Database.Schema.Object`, and `Database..Object`.
- Database-wide shortcut `Database.fragment` searches schemas and schema-qualified
  objects across schemas while preserving legal insertion.
- Four-part/Linked Server intelligence is out of scope.
- Curated developer metadata includes selected `sys` views and common
  `INFORMATION_SCHEMA` views without broad system-object noise.

### QueryScopes And Document-Local Semantics

- QueryScopes are nested, local-first, and position-sensitive.
- Legal correlated outer aliases are visible in expression subqueries.
- Sibling scopes and future aliases are isolated.
- Alias shadowing is local-first.
- Ordinary derived tables do not correlate.
- `CROSS APPLY` and `OUTER APPLY` may see left-side sources.
- CTEs support projections, explicit column lists, ordering of definitions, and
  conservative recursive behavior.
- Temp tables, table variables, `SELECT INTO`, straightforward `ALTER TABLE ADD`,
  derived tables, `VALUES`, and APPLY projections expose local RowSources.
- Projection aliases are visible where SQL Server permits them, notably `ORDER BY`.
- Set operations (`UNION`, `UNION ALL`, `INTERSECT`, `EXCEPT`) compose result
  columns by ordinal using the first branch's names.

### Schema Intelligence

- Catalog metadata includes schemas, tables, views, scalar functions, TVFs,
  procedures, parameters, types, synonyms, sequences, physical columns, PK, UQ,
  unique indexes, FKs, and relationship mappings where supported.
- Physical columns display compact roles: `PK`, `UQ`, `FK`, and combinations.
- Composite constraints preserve ordinal mapping.
- Filtered unique indexes remain distinguishable from ordinary unique constraints.
- INCLUDE columns are not unique-key members.
- Principal columns are not incorrectly marked FK due to incoming references.
- Same-named objects in different schemas/databases remain isolated.

### JOIN Intelligence

- FK JOIN predicates are generated from actual enabled cached FK metadata.
- Current-right alias is rendered first.
- Reverse query order works.
- Multiple FKs between the same tables remain separate candidates.
- Composite FKs produce one ordered predicate.
- Cross-schema FKs work inside one database.
- Disabled FKs and unrelated same-name/type heuristics do not generate predicates.
- JOIN source ranking boosts related tables after Contains filtering.
- FK relationship inference does not cross database boundaries.
- ON-clause visibility is positional and cannot see future JOIN aliases.

### DML And Callable Objects

- INSERT target-list completion offers writable target columns.
- UPDATE SET LHS offers writable target columns.
- Identity, computed, generated, hidden, and rowversion columns are excluded from
  writable target suggestions.
- UPDATE RHS, INSERT VALUES, and INSERT SELECT can derive ExpectedType.
- DELETE scope stays statement-correct.
- OUTPUT supports valid `inserted`/`deleted` pseudo sources with full column
  metadata.
- Invalid pseudo source contexts are not broadened.
- EXEC/EXECUTE named parameter completion preserves declaration order, excludes
  already used parameters, and displays OUTPUT parameters.
- Scalar UDF and TVF Signature Help use shared callable parsing.
- Stored-procedure result-set discovery remains deliberately unsupported.

### Type Intelligence

- Normalized SQL types preserve facets such as length, precision, scale,
  nullability, and UDT identity.
- Compatibility tiers cover exact, same base type, compatible family, unknown,
  and incompatible.
- Physical/query-local columns, literals, CAST, CONVERT, scalar UDF returns,
  arithmetic, conservative CASE, ISNULL/COALESCE where recognized, and set results
  can participate in type inference.
- ExpectedType sources include comparison operands, function arguments, UPDATE,
  INSERT, LIKE, and arithmetic.
- Type-aware completion ranks rather than hides visible candidates.
- Without a reliable ExpectedType, no type groups or type sorting are applied.
- User-facing groups may be `Type match · <type>`, `Compatible <family>`, and
  `Other visible columns`.

### Built-In Function Intelligence

- Static built-in catalog is immutable, case-insensitive, connection-independent,
  and does not initialize schema metadata.
- Supported built-ins: `CHARINDEX`, `DATEADD`, `DATEDIFF`, `DATEFROMPARTS`,
  `ROUND`, `STRING_AGG`, `SUBSTRING`.
- Built-ins appear in expression completion, not row-source completion.
- Built-ins use shared call-site parsing, Signature Help, ExpectedType, and return
  inference.
- SQL Server 2022 semantics are used; `DATEADD` bigint-preview behavior is not
  treated as available.

### Productivity

- `SELECT *` and `alias.*` can expand with Tab using semantic RowSource columns.
- Smart Alias suggestions appear only after a syntactically complete RowSource and
  separating whitespace or `AS`.
- Smart Alias suggestions insert only the alias snippet, never `AS` or the
  RowSource.
- Existing aliases suppress redundant alias suggestions.
- Collision fallback is deterministic and scoped to legally visible QueryScopes.
- Exact shorter object names must not suppress longer legal Contains candidates.

## Existing Fixture Coverage And Gaps

### What Is Already Strong

- `tests/fixtures/create-schema-intelligence-fixture.sql` covers `reltest` and
  `relref` for PK/UQ/FK metadata, three parallel address FKs, composite
  PK/composite FK, cross-schema FK, filtered unique index, INCLUDE safety,
  disabled/untrusted FK state, and relationship-based JOIN tests.
- Unit/provider/Extension Host tests cover the semantic contracts without a live
  database.
- `tests/persistent-metadata.test.ts` covers the persistent 0.11.0 lifecycle with
  deterministic stores and injectable time.

### Current Fixture Gaps

The repository does not currently contain one complete SQL provisioning script for
all manual acceptance features. Live integration and Extension Host tests expect
several real/manual fixtures that are not fully created by the two checked-in SQL
scripts:

- Active database base objects such as richer `dbo.Customers`,
  `sales.CustomerOrders`, `dbo.CustomerAddresses`, `billing.BillingAddress_0001`,
  `billing.CalculateBillingTotal_0001`, `reporting.GetCustomerAddresses_0001`,
  `dbo.FindCustomerAddress`, `reltest.CompletionLayoutStress`, and
  `reltest.TypedTargets`.
- Prefix-family tables `Belege`, `BelegePositionen`,
  `BelegePositionenDetails` for exact/prefix collision and Smart Alias tests.
- Secondary database richer reporting objects:
  `Customers` with reporting-specific columns, `ActiveCustomerAddresses`,
  `GetCustomerAddresses`, `GetBillingsByCustomer`, `archive.CustomerAddressArchive`,
  `Auftraege`, and `AuftraegePositionen`.
- `tests/fixtures/create-cross-database-fixture.sql` is minimal and does not match
  all current live integration expectations; for example it creates a small
  `dbo.Customers` table while tests expect richer reporting columns in some paths.

### Why The Supplemental Fixture Uses New Schemas

The supplemental script creates new neutral `qpacc`, `qpacc_ref`, and
`qpacc_archive` objects instead of modifying existing `dbo`, `reltest`, `relref`,
`billing`, `sales`, `reporting`, or `archive` fixtures. This avoids disturbing
fixtures that already work for automated or installed-editor tests and gives
manual acceptance a controlled namespace.

The script creates `qpacc.OrderLines` independently from its composite primary and
foreign keys, then adds each constraint through its own idempotent guard. This
prevents a dependency-binding failure from rolling back the base table creation.
Explicit postconditions report a missing table or constraint, and each database
section finishes with a complete expected-object audit so a partial provisioning
run cannot appear successful.

## Supplemental Fixture Rationale

| Fixture                                                         | Feature/test gap covered                                                                                     |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `IntelliSenseLab.qpacc_ref.Regions`                             | Cross-schema FK target, schema role metadata, related-object graph                                           |
| `IntelliSenseLab.qpacc.Addresses`                               | Principal table for three distinct customer-address FKs                                                      |
| `IntelliSenseLab.qpacc.Customers`                               | Alias members, PK/UQ/FK roles, filtered UQ, bigint/string/GUID/date/decimal ExpectedType, DML pseudo sources |
| `IntelliSenseLab.qpacc.OrderHeaders`                            | Composite PK principal, single FK to customer, JOIN predicate order, bigint comparison                       |
| `IntelliSenseLab.qpacc.OrderLines`                              | Composite FK dependent, decimal argument/INSERT ExpectedType                                                 |
| `IntelliSenseLab.qpacc.CustomerAliases`                         | UQ+FK role combination and cascade relationship metadata                                                     |
| `IntelliSenseLab.qpacc.Products`                                | Unrelated JOIN negative case and INCLUDE-column unique index safety                                          |
| `IntelliSenseLab.qpacc.LegacyCustomerLinks`                     | Disabled/untrusted FK negative relationship case                                                             |
| `IntelliSenseLab.qpacc.CompletionLayoutStress`                  | Long-name display, canonical insertion, unusual types, rowversion/computed/identity writable exclusion       |
| `IntelliSenseLab.qpacc.TypedTargets`                            | Stable INSERT/UPDATE ExpectedType target table                                                               |
| `IntelliSenseLab.qpacc.Belege*`                                 | Prefix-family Contains, Smart Alias, alias collisions, simple FK JOIN predicates                             |
| `IntelliSenseLab.qpacc.CalculateBillingTotal_Manual`            | Scalar UDF Signature Help, argument ExpectedType, scalar return metadata                                     |
| `IntelliSenseLab.qpacc.GetCustomerAddresses_Manual`             | TVF Signature Help and row-source callable metadata                                                          |
| `IntelliSenseLab.qpacc.FindCustomerAddress_Manual`              | EXEC named parameters, used exclusion, OUTPUT parameter                                                      |
| `IntelliSenseLabReporting.qpacc.Customers`                      | Same-server secondary database alias members and cross-db set/CTE identity                                   |
| `IntelliSenseLabReporting.qpacc.ActiveCustomerAddresses`        | Secondary database view completion                                                                           |
| `IntelliSenseLabReporting.qpacc.CustomerAddressReport`          | Secondary database reporting view/object discovery                                                           |
| `IntelliSenseLabReporting.qpacc.GetCustomerAddresses`           | Secondary database TVF completion                                                                            |
| `IntelliSenseLabReporting.qpacc.Auftraege*`                     | Cross-database prefix-family and Smart Alias checks                                                          |
| `IntelliSenseLabReporting.qpacc_archive.CustomerAddressArchive` | Cross-database CTE/star projection isolation                                                                 |

## Manual Acceptance Matrix

| Area                          | Tests            | Acceptance intent                                                                                     |
| ----------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------- |
| Matching/object navigation    | 01-05, 59-62, 66 | Contains, schema/database qualification, prefix collision, cross-db boundaries                        |
| Smart Alias                   | 06-08            | Alias phase boundary, AS handling, deterministic collision                                            |
| Physical columns/presentation | 09-12            | Explicit alias strictness, PK/UQ/FK metadata, long names, no ExpectedType order                       |
| Type Intelligence             | 13-22            | Comparison, built-in, UDF, UPDATE, INSERT ExpectedType ranking without hiding candidates              |
| DML/callables                 | 23-29, 57-58     | Writable filtering, EXEC params, pseudo sources, scalar/TVF Signature Help                            |
| JOIN Intelligence             | 30-38            | FK predicates, alternatives, composite/cross-schema, negatives, source ranking, positional visibility |
| QueryScopes/local semantics   | 39-52            | CTEs, temp/table variables, derived/VALUES/APPLY, correlation, alias visibility, set operations       |
| Productivity                  | 53-54            | Tab-only wildcard expansion                                                                           |
| Built-ins                     | 55-56            | Static built-in completion and native Signature Help                                                  |
| Cross database                | 59-65, 70        | Same-server secondary metadata, alias identity, CTE/set isolation, lazy load                          |
| Persistent cache lifecycle    | 67-70            | Cold load, warm hydrate, manual refresh, secondary lazy cache                                         |

## Expected Results

### Tests 01-05: Matching And Qualification

- 01: Row-source candidates include `Addresses` and `ActiveCustomerAddresses`.
  Contains is case-insensitive and not StartsWith-only.
- 02: Candidates are schemas only and include `qpacc`, `qpacc_ref`, `dbo`, `sys`,
  and `INFORMATION_SCHEMA`.
- 03: Candidates include schema-qualified matching row sources across schemas,
  including `qpacc.Addresses` and `qpacc.ActiveCustomerAddresses`; database items
  must not appear.
- 04: Candidates are strict to `IntelliSenseLab.qpacc` and include `Addresses` and
  `ActiveCustomerAddresses`.
- 05: Candidates are `Belege`, `BelegePositionen`, and
  `BelegePositionenDetails`. Longer legal Contains matches remain visible while
  the shorter exact identifier is active.

### Tests 06-08: Smart Alias

- 06: One Smart Alias candidate: `bp`.
- 07: One Smart Alias candidate: `bp`.
- 08: One Smart Alias candidate with collision fallback: `bpd2`.
- In all three cases, accepting the candidate inserts only the alias snippet at an
  empty cursor range.

### Tests 09-12: Physical Columns

- 09: Candidates are only members of `qpacc.Customers`; address-fragment matches
  include `BillingAddressId`, `EmailAddress`, `PrimaryAddressId`, and
  `ShippingAddressId`.
- 10: Candidates include `CustomerId` with `PK`, `CustomerNumber` with `UQ`, and
  address columns with `FK`. Documentation for FK columns names the target table.
- 11: Candidates include `ExternalReference` and
  `VeryLongERPBusinessTransactionPostingReferenceIdentifier`. The long name is
  visually shortened but `filterText`/inserted text/documentation keep the full
  identifier.
- 12: No type group headers appear. Column candidates are alphabetical by semantic
  name: `Amount`, `BinaryPayload`, `Code`, `ComputedDisplayName`, `CustomerId`,
  `DisplayName`, `ExternalReference`, `Id`, `OccurredAt`, `Payload`, `RowVersion`,
  `UniqueCustomerId`, `VeryLongERPBusinessTransactionPostingReferenceIdentifier`.

### Tests 13-22: Type Intelligence

- 13: Bigint-compatible customer columns rank first. The strongest first group
  contains `BillingAddressId`, `CustomerId`, `PrimaryAddressId`, and
  `ShippingAddressId`; `RegionId` ranks before string/GUID columns; incompatible
  visible columns remain present.
- 14: `CustomerNumber` ranks before unrelated columns; compatible string-family
  columns such as `DisplayName`/`EmailAddress` rank above incompatible GUID/date
  columns.
- 15: `ExternalKey` ranks first and displays `uniqueidentifier` without fake
  facets.
- 16: `OccurredAt` ranks first or in the strongest date/time-compatible group;
  incompatible columns remain visible below.
- 17: Integer-family columns such as `RegionId` rank above string/GUID columns.
- 18: String-family columns such as `CustomerNumber`, `DisplayName`, and
  `EmailAddress` rank above incompatible columns.
- 19: Decimal/numeric columns from `OrderLines`, especially `Quantity` and
  `AmountExact`, rank above unrelated columns.
- 20: `ExternalKey` ranks first; type group header should read
  `Type match · uniqueidentifier` when grouping is shown.
- 21: The second assignment still uses `ExternalReference`; `ExternalKey` ranks
  first despite the preceding `CustomerId` assignment.
- 22: `Quantity` and/or `AmountExact` rank above unrelated columns for the
  `Amount` target.

### Tests 23-29: DML And Procedures

- 23: Writable target suggestions include `ExternalReference`. They do not include
  `Id`, `RowVersion`, or `ComputedDisplayName`.
- 24: Same writable filtering as 23.
- 25: Parameter candidates in declaration order:
  `@Search`, `@MaxRows`, `@RowsAffected`; `@RowsAffected` is marked OUTPUT.
- 26: `@Search` is excluded; remaining candidates are `@MaxRows`,
  `@RowsAffected`.
- 27: `inserted.` exposes all physical `qpacc.Customers` columns, including
  non-writable identity `CustomerId`, with canonical metadata.
- 28: `deleted.` exposes all physical `qpacc.Customers` columns with canonical
  metadata.
- 29: No semantic Query Puppy columns are returned for invalid `inserted` in a
  DELETE statement.

### Tests 30-38: JOIN Intelligence

- 30: Predicate candidate: `oh.CustomerId = c.CustomerId`.
- 31: Predicate candidate: `c.CustomerId = oh.CustomerId`.
- 32: Three separate predicate candidates, one for each customer address FK:
  billing, primary, and shipping.
- 33: Predicate candidate:
  `ol.CompanyId = oh.CompanyId AND ol.OrderId = oh.OrderId`.
- 34: Predicate candidate: `r.RegionId = c.RegionId`.
- 35: No FK predicate from the disabled/untrusted relationship.
- 36: No FK predicate for unrelated `Products`.
- 37: Related tables such as `Addresses`, `OrderHeaders`, and `CustomerAliases`
  rank above unrelated `Products` after Contains/filtering rules are applied.
- 38: At the first `ca.` no columns appear because that alias is a future source.
  At the second `ca.`, address columns are available.

### Tests 39-52: QueryScopes And Local Semantics

- 39: `x.` exposes `BillingAddressId`, `CustomerId`, and `EmailAddress`.
- 40: `x.` exposes explicit names `AddressValue` and `EntityId`.
- 41: `t.` exposes `BillingAddressId` and `CustomerId`, with known bigint metadata
  where applicable.
- 42: `v.` exposes `WorkCode` and `WorkId`; scalar variables are not row-source
  members.
- 43: `d.` exposes `Contact` and `Id`.
- 44: `v.` exposes `ValueId` and `ValueName`.
- 45: `lastOrder.` exposes `OrderId` and `OrderNumber`.
- 46: `c.` is visible from the correlated subquery and exposes customer columns.
- 47: Ordinary derived-table inner query does not correlate; no customer-column
  candidates from `c.` should appear there.
- 48: Sibling scope alias `oh` is not visible in the second EXISTS; no order columns
  should appear.
- 49: `Contact` is visible in `ORDER BY`.
- 50: Projection alias `Contact` is not visible in `GROUP BY`; no semantic
  candidate is expected.
- 51: `x.` exposes first-branch set-result names `Id` and `Value`, not the second
  branch aliases.
- 52: `c.` from a previous set branch is not visible in the second branch; no
  customer-column candidates should appear.

### Tests 53-54: Wildcard Expansion

- 53: Pressing Tab after `c.*` replaces the wildcard with qualified customer
  columns in source order. Pressing Enter must not expand.
- 54: Pressing Tab after `*` expands columns from both visible sources in
  deterministic source/column order using safe qualification. Pressing Enter must
  not expand.

### Tests 55-58: Built-Ins And Signature Help

- 55: Built-in function candidates are `DATEADD`, `DATEDIFF`, and
  `DATEFROMPARTS`.
- 56: Signature Help opens for `DATEADD`; active parameter is the first parameter
  `datepart`.
- 57: Signature Help label is
  `qpacc.CalculateBillingTotal_Manual(@NetAmount decimal(18,2), @TaxRate decimal(9,4)) → decimal(18,2)`;
  active parameter is `@NetAmount`.
- 58: Signature Help labels the TVF as returning `table`; active parameter is
  `@CustomerId bigint`.

### Tests 59-66: Cross-Database

- 59: Database candidates include `IntelliSenseLab` and
  `IntelliSenseLabReporting`.
- 60: Schema candidates in the secondary database include `qpacc` and
  `qpacc_archive`.
- 61: Candidates strict to `IntelliSenseLabReporting.qpacc` include
  `Customers`, `CustomerAddressReport`, `ActiveCustomerAddresses`, and
  `GetCustomerAddresses`, with tables/views/TVFs in their semantic groups.
- 62: `r.` exposes secondary customer columns:
  `CustomerDisplayName`, `CustomerNumber`, `LastReportedAt`, `ReportAddressId`,
  `ReportingCustomerId`, `ReportingEmailAddress`, `ReportingGroup`,
  `SnapshotDate`.
- 63: At `c.` only active database customer columns appear; at `r.` only secondary
  database reporting customer columns appear.
- 64: `r.` candidates all belong to `IntelliSenseLabReporting`.
- 65: `y.` exposes only archive projection columns from the secondary database,
  not active customer projection columns.
- 66: Four-part names are outside scope; no Query Puppy semantic candidates are
  expected.

### Tests 67-70: Persistent Metadata Cache

- 67: After **Clear Schema Cache for Active Database**, the first completion shows
  visible cold-load status and eventually returns `qpacc.Customers` columns.
- 68: After editor/Extension Host restart and reconnect, `oh.` returns cached
  columns immediately while a background refresh may be visible.
- 69: During **Refresh Schema Metadata**, existing `ol.` completion remains usable;
  old metadata is not discarded while refresh is running.
- 70: The first explicit secondary database use triggers its own lazy load/hydrate
  lifecycle without loading unrelated databases.

## Residual Manual Notes

- The persistent lifecycle is partly observable through status bar/output, not only
  through SQL candidates.
- Native Suggest Widget layout, text clipping, group headers, Signature Help popups,
  and Tab wildcard expansion require installed editor or Extension Host acceptance.
- The supplemental fixture script grants metadata visibility to `intellisense_test`
  only if that login/principal already exists; otherwise use an account that can
  read catalog metadata.
